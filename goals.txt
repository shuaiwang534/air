用户300页技术文档 --> 模版化 --> 构建知识库，嵌入为向量 --> 根据用户指令检索知识库向量 --> 任务流生成md文档 --> java脚本转化为MagicDraw对象，并交给Open API生图

1. Word 文档
   ↓
2. 章节树构建（Heading 结构）
   ↓
3. 章节级 chunk
   ↓
4. 语义子块切分（包含关键接口、关键逻辑、关键组成、关键特征）
   ↓
5. 候选识别（Candidate）
   ↓
6. 候选归一 & 字段补全 → Object
   ↓
7. 双知识库构建
   - Chunk库（原文证据）
   - Object库（工程要素）
   ↓
8. 选择性 embedding
   ↓
9. 向量检索
   ↓
10. 输出：
    - TopK 证据
    - TopN 工程对象

================================================================================================



4. 语义子块切分（包含关键接口、关键逻辑、关键组成、关键特征）
semantic_pipeline/
├── splitter/
│   ├── paragraph_split.py      # 基于段落/标点的初切
│   ├── heuristic_split.py      # 规则触发切分（冒号、枚举等）
│
├── llm/
│   ├── client.py               # LLM 抽象接口（重点）
│   ├── prompt_templates.py     # 所有 prompt 统一管理
│   └── classifiers.py          # 语义块判别逻辑
│
├── semantic_block/
│   ├── models.py               # SemanticBlock 数据结构
│   └── builder.py              # Step 4 主流程
│
├── demo.py                     # 你现在要跑的 demo

章节 content
  ↓
候选块生成（规则 / 段落）
  ↓
LLM 判定（是不是单一工程语义）
  ↓
LLM 分类（Feature / Logic / ...）



================================================================================================


# ================================
# 基础模型（本地 GGUF）
# ================================
FROM ./Qwen2.5-7B-Instruct-Q4_0.gguf


# ================================
# Chat Template（必须）
# ================================
TEMPLATE """
<|im_start|>system
{{ .System }}
<|im_end|>

<|im_start|>user
{{ .Prompt }}
<|im_end|>

<|im_start|>assistant
"""


# ================================
# 推理参数（工程判别优化）
# ================================
# 偏裁判，不偏生成
PARAMETER temperature 0.35
PARAMETER top_p 0.9
PARAMETER top_k 50
PARAMETER min_p 0.1

# JSON 稳定性
PARAMETER repeat_penalty 1.15
PARAMETER presence_penalty 0.0

# 上下文
PARAMETER num_ctx 4096


# ================================
# 停止符（必须）
# ================================
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"


# ================================
# SYSTEM：工程语义结构裁判
# ================================
SYSTEM """
你是一个【工程语义结构裁判（Semantic Block Judge）】。

你的任务是对输入的文本段落进行工程语义分析，评估文本中的语义块。无论文本是否包含工程语义，都必须返回有效的 JSON 格式,不得包含任何解释。

- 如果段落内容是对图像或表格等非文本内容的引用，判定为 `Other` 类型，不得返回空数组。
- 只有当段落中同时包含多个不同类型的工程语义时，才进行拆分。

--------------------------------
一、block_type 定义（严格）
--------------------------------

1. Component（组成 / 实体）
仅当文本在描述系统的物理或逻辑组成单元本身，
而不描述其行为、处理方式或运行逻辑时，判为 Component。
典型信号：
- “由……组成”
- “包括……模块 / 单元 / 计算机”
- 明确名词性实体罗列

------------------------------------------------

2. Interface（接口 / 交互边界）
仅当文本明确描述：
- 人与系统之间
- 系统与系统 / 子系统之间
的输入、输出、信号或指令交互关系时，判为 Interface。
典型信号：
- “输入 / 输出”
- “发送 / 接收”
- “通过……向……”
- “与……交互”

------------------------------------------------

3. Logic（逻辑 / 机制 / 运行规则）
当文本描述系统的处理方式、控制律、计算规则、
冗余策略、模式切换、因果或条件关系时，判为 Logic。
典型信号：
- “进行……计算”
- “在……模式下”
- “当……时”
- “采用……策略”

------------------------------------------------

4. Feature（功能 / 能力）
当文本描述系统具备的能力、功能或监控项，
但不涉及其实现机制或处理流程时，判为 Feature。
典型信号：
- “具备……功能”
- “支持……”
- “对……进行监控”

------------------------------------------------

5. Other（非工程语义正文）
以下内容必须判为 Other，且不得拆分：
- 表格内容（如缩略语表、参数表、对照表）
- 图注 / 表注
- 规范性、引导性或背景性说明
- 与工程要素建模无关的描述
- 表格、图表、图片等非文本内容的描述归为others，不能返回空内容（重要）

--------------------------------
二、拆分规则（非常重要）
--------------------------------

- 默认：一个段落只生成一个 SemanticBlock
- 只有当一个段落中【同时存在 block_type 不同的独立语义】时，才拆分
- 不得因为句号、分号、换行而机械拆分
- 不得为了数量而拆分

--------------------------------
三、content 规则
--------------------------------

- 每个 block 的 content 必须是【原文中的连续子串】
- 不得改写、补写、总结或推断
- 拆分后内容顺序必须与原文一致

--------------------------------
四、输出格式（严格）
--------------------------------

- 只输出 JSON
- 如果不需要拆分，输出单个 JSON 对象
- 如果需要拆分，输出 JSON 数组
- 不输出任何解释性文字

字段结构（当前阶段最小结构）：

{
  "block_type": "Component | Interface | Logic | Feature | Other",
  "content": string,
  "confidence": number   // 0.0 ~ 1.0
}

"""

================================================================================================

Step 2｜对象候选抽取（不谈关键）

输入：SemanticBlock
输出：对象候选（Candidate Object）

按 block_type 映射：

block_type	抽取的候选对象
Component	Component 候选
Interface	Interface 候选
Feature	Function 候选
Logic	LogicRule 候选
Other	忽略

👉 原则：

抽全

允许重复

不判断重要性


================================================================================================
候选归一
{
  "section_id": "9.5.1",
  "chunk_id": "CH_9.5.1_001",
  "object_refs": [
    {"object_id":"OBJ_COMP_001"},
    {"object_id":"OBJ_ACT_002"},
    {"object_id":"OBJ_CTRL_003"},
    {"object_id":"OBJ_CTRL_004"},
    {"object_id":"OBJ_BUS_005"},
    {"object_id":"OBJ_PWR_006"}
  ],
  "relations": [
    {"type":"ControlLink","source":"OBJ_CTRL_003","target":"OBJ_ACT_002","predicate":"controls"},
    {"type":"CommInterface","endpoints":["OBJ_CTRL_003","OBJ_CTRL_004"],"medium":"OBJ_BUS_005","signal":"communication"},
    {"type":"PowerLink","source":"OBJ_PWR_006","target":"OBJ_CTRL_003","predicate":"powers"}
  ],
  "evidence": {"chunk_id":"CH_9.5.1_001"}
}


[
  {
    "object_id":"OBJ_CTRL_003",
    "canonical_name":"REU",
    "object_type":"Controller",
    "aliases":["REU"],
    "description": null,
    "evidence_chunks":["CH_9.5.1_001"]
  }
]




